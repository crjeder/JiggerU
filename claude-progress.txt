# Claude Progress

## 2026-03-13 — Session: Ambiguity resolution (label-first ingredient matching)

### Task Completed
- Added label-first matching to `resolveDispensableIngredients()` in `DispenseWorkflow.js`: when a recipe ingredient has a `label`, the bar entry whose `ingredient` matches the label is preferred over a generic type match; falls back silently if no match
- Exported `resolveDispensableIngredients` and added 6 unit tests in `DispenseWorkflow.spec.js`
- Updated CHANGELOG.md, TODO.md (ambiguity resolution marked done)
- Created openspec change `ambiguity-resolution` with full artifacts

### Files Changed
- src/components/CocktailPage/DispenseWorkflow.js
- src/components/CocktailPage/DispenseWorkflow.spec.js (created)
- CHANGELOG.md, TODO.md, claude-progress.txt
- openspec/changes/ambiguity-resolution/ (new change + all artifacts)

### Branch
feat/card-mix-button

## 2026-03-10 — Session: Add "Mix it!" button to cocktail card

### Task Completed
- Added `mixButtonLabel` to `theme.custom` in all 4 theme files (classic: "Mix it!", c64: "RUN", negroni: "Mix it!", blossom: "Mix it!")
- Wired `CocktailCard.js` with robot-readiness selectors, `DispenseWorkflow` import, `canDispense` logic, and `CardActions` footer
- Button is only rendered when robot is connected, idle, and the cocktail has at least one dispensable ingredient
- Button text reads from `theme.custom?.mixButtonLabel ?? "Mix it!"` via `useTheme()`
- All 65 tests pass; snapshots unchanged (button hidden by default in test store)

### Files Changed
- src/components/CocktailCard.js
- src/themes/classic/theme.js, c64/theme.js, negroni/theme.js, blossom/theme.js
- CHANGELOG.md, TODO.md, claude-progress.txt
- openspec/changes/card-mix-button/ (new change + all artifacts)

### Branch
feat/card-mix-button (new)

## 2026-03-10 — Session: Remove favourites logic and Learn More button

### Task Completed
- Removed entire Favourites feature: `UPDATE_FAVOURITES` action type, `updateFavourites` action creator, `favourites` reducer state + case, `state.favourites` persistence in store, `favouritesSelector` + `isFavouriteSelector` selectors
- Deleted `src/components/CocktailActions.js` (contained heart toggle + Learn More button)
- Removed `CardActions` block from `CocktailCard.js` and cleaned up Redux connection
- Removed "Actions" column from `TableView.js`
- Removed `favouritesOnly` filter from `filterConfig.js` and `isFavourite` rule from `filterRules.js`
- Removed `favouritesOnly` chip label from `FilterChips.js`
- Removed related tests from `actions.spec.js` and `selectors/selectors.spec.js`
- Regenerated snapshots; all 65 tests pass
- Updated `CLAUDE.md` and `openspec/config.yaml` to remove stale favourites references
- PR #8: https://github.com/crjeder/JiggerU/pull/8

### Files Changed
- src/actionTypes.js, src/actions.js, src/actions.spec.js
- src/components/CocktailActions.js (deleted)
- src/components/CocktailCard.js, TableView.js, Filters/FilterChips.js
- src/components/__snapshots__/CardView.spec.js.snap, TableView.spec.js.snap
- src/filterConfig.js, src/reducers/index.js, src/selectors/index.js
- src/selectors/selectors.spec.js, src/store.js, src/utilities/filterRules.js
- CLAUDE.md, openspec/config.yaml, TODO.md
- openspec/changes/archive/2026-03-10-remove-favourites-and-learn-more/ (new)

